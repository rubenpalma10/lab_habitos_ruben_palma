name: lab_habitos_ruben_palma
description: "Panel de hábitos del día — E2_L1: interfaz interactiva con actualización dinámica (StatefulWidget + setState)."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
